---
"@hmr-lukas/heft-tailwind-plugin": minor
---

Detect SPFx versions during initialization, use a loader-compatible `.global.scss` output for SPFx 1.23, order Sass after Tailwind, migrate existing generated stylesheet imports, prompt before configuring newer SPFx versions, and add CLI version output.
